echo $TRAVIS_COMMIT_MESSAGE $TRAVIS_BUILD_WEB_URL $TRAVIS_COMMIT_MESSAGE $PWD;
netlify deploy --dir=./build --prod --site $NETLIFY_SITE_ID_PROD --auth $NETLIFY_AUTH_TOKEN -m "${TRAVIS_COMMIT} PR: ${TRAVIS_PULL_REQUEST}"